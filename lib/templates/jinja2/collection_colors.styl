/*
    Collection colors generated by marketplace-constants.
*/
{% for color, hex in colors %}
$color-collection--{{ color }} = {{ hex }};
{%- endfor %}

$collection-colors = {% for color, hex in colors %}{{ color }} {% endfor %};
